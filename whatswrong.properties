version=0.0.4
name=whatswrong
target=${name}-${version}
jarfile=${target}.jar
distdir=dist
fulltarget=${distdir}/${target}
fulltargetgz=${distdir}/${target}.tar.gz
fulltargetzip=${distdir}/${target}.zip
